# Koala runtime dependencies. Install from the repository root:
#   python -m pip install -e .
# JAX selects the CPU backend by default. For NVIDIA GPUs, install the
# appropriate JAX CUDA extra as described in docs/install.md.

# JAX computation, inference, optimization, and transit models
jax>=0.5.3
jaxlib>=0.5.3
jaxoplanet>=0.1.0
numpyro>=0.15.3
numpyro-ext>=0.0.4
jaxopt>=0.8.3
# tinygp comes from the Koala fork (upstream dfm/tinygp main with the parallel
# associative-scan quasiseparable solver; branch koala-stable, pinned by commit).
tinygp @ git+https://github.com/TylerFair/tinygp.git@96d110c8fb4350b0daf50f7f1f86a9196b82fcd4

# Harmonica is compiled into the Koala distribution from harmonica_modified/.
# These requirements are also read by pyproject.toml during pip installation.

# Scientific arrays, FITS/CSV input, configuration, and limb darkening
numpy
scipy
astropy
pandas
PyYAML
# ExoTiC-LD 3.2.0 on PyPI lacks compute_power2_ld_coeffs, which Koala's
# power-2 limb darkening needs. Upstream added it in the v3.2.2 tag, which is
# not on PyPI, so that commit is pinned (it reports itself as 3.2.1).
exotic-ld @ git+https://github.com/Exo-TiC/ExoTiC-LD.git@ef7eba2d5ec32818b031714301858c4df7bdb16d
# ExoTiC-LD 3.2 uses pkg_resources, removed in setuptools 81.
setuptools<81

# Diagnostics and publication-style plots
matplotlib
corner
arviz
SciencePlots
cmcrameri

# ExoTEDRF is not required; spectral binning is included in koala.binning.
# Test tools and Sphinx dependencies are separate from runtime requirements.
