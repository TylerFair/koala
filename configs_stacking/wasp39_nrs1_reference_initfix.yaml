dataset: wasp39_nrs1_ref
analysis_stage: all
variants:
  - name: wide_linear_initfix
    overrides:
      input_dir: /scratch/midway3/tfairnington/FITS
      flags: {ld_prior: widegaussian, ld_profile: power2, detrending_type: linear, whitelight_mass_matrix: laplace}
  - name: uniform_linear_step_initfix
    overrides:
      input_dir: /scratch/midway3/tfairnington/FITS
      flags: {ld_prior: uniform, ld_profile: power2, detrending_type: linear_discontinuity, whitelight_mass_matrix: laplace}
